import 'package:flutter/material.dart';

// Patient List Page
class PatientListScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Patient List Page", style: TextStyle(fontSize: 24)),
    );
  }
}
